import React, { Component } from 'react'

export default class Button extends Component {
    render() {
        return (
            <div style={{marginLeft: "10px"}}>
                <button>다음</button>
                <a href="" style={{marginLeft: "20px"}}>로그인</a>
            </div>
        )
    }
}
